#include<stdio.h>
#include<math.h>
int main()
{
	int n,rem=0,res=0,i;
	printf("enter the number")
	scanf(%d,&n);
	while(n!=0)
	{
		rem=n/2;
		n=n/2;
		res=res+rem*pow(10,i);
		printf(%d,rem);
	]
	return(0)
}
